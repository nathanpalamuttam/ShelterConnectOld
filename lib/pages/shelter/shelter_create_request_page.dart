import 'package:flutter/material.dart';

class CreateRequest extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is the page where u make the requests")
    );
  }
}
